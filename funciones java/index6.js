function esVocal(p){
   
    if ( p === 'a' || p === 'e' || p === 'i' || p === 'o' || p === 'u') {
       console.log (`la letra ${p} es una vocal`);
    } else if (p === 'A' || p === 'B' || p === 'I' || p === 'O' || p === 'U') {
       console.log (`la letra ${p} es una vocal`);
    }
    else{
        console.log (`la letra ${p} es una vocal`);
    }
}


esVocal('a')
esVocal('B') 
esVocal('c')
esVocal('E')
esVocal('u')